node('docker-jenkins-slave') {
  stage 'Build and unit tests'
  echo 'launch unit tests'
  stage 'Static code analysis'
  echo 'launch shellcheck and export checkstyle'
  echo 'Failed if threshold not met'
  stage 'Deploy'
  echo 'Deploy'
}
